export interface ExpenseRequestMetrics {
  total: number;
  pending: number;
  approved: number;
  rejected: number;
  totalValue: number;
  approvedValue: number;
  pendingValue: number;
  rejectedValue: number;
}
